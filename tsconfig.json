{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    /* ... 기존 설정들 ... */

    "skipLibCheck": true, // 라이브러리(.d.ts) 파일의 타입 검사를 건너뜁니다.
    "isolatedModules": true
  },
  "exclude": [
    "node_modules" // node_modules 폴더는 검사에서 제외합니다.
  ]
}
